import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'social.media.app',
  appName: 'social-media-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
